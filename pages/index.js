function Home(){ //D4 - H maiúsculo que é aconvenção do React para componentes
    return <h1>Projeto para curso.dev</h1>
}

export default Home
// D4 - cola entre next e react
// D4 - export default - como sinalizamos para o next js que é a função Home que queremos usar para renderizar a página sendo que neste arquivo pode haver diversas funções
